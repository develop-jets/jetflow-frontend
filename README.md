# jetflow-frontend

- created next application
- folder structure inside src/
    ├─ app/               # Next.js routing (already there)
    ├─ components/        # Reusable UI components (buttons, forms, navbars)
    ├─ layouts/           # Page layouts (sidebar, dashboard shell)
    ├─ modules/           # Feature-specific code (auth, orchestrator, logs, etc.)
    ├─ lib/               # Utilities (API clients, helpers)
    ├─ styles/            # Global styles (if any extra)
    └─ types/             # TypeScript types & interfaces
- created default landing page